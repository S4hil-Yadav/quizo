{
  "name": "lets-connect",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "build": "npm i --prefix frontend && npm i --prefix backend && npm run build --prefix frontend",
    "f": "npm run dev --prefix frontend",
    "b": "npm run dev --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "react-slick": "^0.30.3"
  }
}
